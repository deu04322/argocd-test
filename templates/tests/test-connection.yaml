apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "cicd-tester.fullname" . }}-test-connection"
  labels:
    {{- include "cicd-tester.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "cicd-tester.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
